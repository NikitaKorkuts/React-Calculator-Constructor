export enum CalculatorComponentsEnum {
  Display,
  OperationsButtons,
  NumericButtons,
  EqualButton,
}

export enum OperationsEnum {
  None,
  Add,
  Subtract,
  Multiply,
  Divide,
}

export enum ModeEnum {
  Runtime,
  Constructor,
}
